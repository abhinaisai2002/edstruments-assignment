'use client'

import InvoiceDashboard from "@/components/invoice-dashboard"


const Dashboard = () => {
    return (
        <InvoiceDashboard 
            onCreateNew={() => {}} 
            onEditInvoice={() => {}} 
        />
    )
}

export default Dashboard